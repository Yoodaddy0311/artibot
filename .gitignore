node_modules/
.DS_Store
.claude/
*.log
*.tmp
.env
.env.*
temp-repos/
_reference_repos/
_refs/
_analysis/
repos/
refs/
nul
_benchmarks/
blog-artibot-intro.md
# /watch ingest artifacts (video keyframes/transcripts) — local-only, never commit
.artibot/media/
/docs/
plugins/artibot/_reports/
plugins/artibot/_Benchmarks/
# Internal development docs — plugin surface is plugins/artibot/README.md only
# (use /* pattern so we can re-include specific user-facing files)
plugins/artibot/docs/*
# User-facing docs (linked from README) must be tracked
!plugins/artibot/docs/SCHEDULED-JOBS.md
!plugins/artibot/docs/MCP-SETUP.md
!plugins/artibot/docs/PRD-TELEGRAM-MCP.md
!plugins/artibot/docs/PRD-EFFORT-DYNAMIC-WORKFLOW.md
!plugins/artibot/docs/PRD-LEARNING-ACTIVATION.md
!plugins/artibot/docs/WIRING-AUDIT-2026-05-30.md
!plugins/artibot/docs/PRD-DREAMING.md
!plugins/artibot/docs/IMPROVEMENT-DESIGN-2026-07-02.md
!plugins/artibot/docs/MARKETPLACE-SUBMISSION.md
!plugins/artibot/docs/CITATION-SYNTAX.md
!plugins/artibot/docs/mcp-server-usage.md
!plugins/artibot/docs/completion-block-spec.md
# (2026-09-03, 결정 B2) `!plugins/artibot/docs/adr/` + `…/adr/**` 재포함 2줄을 삭제했다.
# ADR 정본이 `.artibot/adr/` 로 옮겨가면서 그 디렉터리가 비었고, 되살릴 대상이 없는
# 재포함은 다음 사람에게 "ADR 이 아직 저기 있다"고 잘못 알린다. `.artibot/adr/` 는
# :116 정본 목록에 있고 규칙이 없어 그대로 추적된다.
plugins/artibot/coverage/
plugins/artibot/image.png
# Runtime state files (token usage, sessions, hashes) — never commit; caused rebase corruption v3.9.0→v3.9.1
plugins/artibot/runtime/
plugins/artibot/.claude-cache/
NVIDIA Corporation/
*.jpeg
*.png
!assets/*.jpeg
!assets/*.png

# Codex CLI auto-generated files
/.agents/
/AGENTS.md
/agents/
CLAUDE.local.md

# Defense against Windows path-as-filename bug (vitest/tmp output misconfig)
C:Users*
*vitest-out.json
*AppDataLocalTemp*

# Squad C (AD-23/24/37/38/40): hook-managed runtime data — never commit
runtime/cache/
runtime/state/
runtime/autopilot/
!runtime/cache/webfetch/.gitkeep
!runtime/state/.gitkeep

# Generated reports / analyses / plugin design proposals (committed history kept; new files ignored)
# reports/* (not reports/) so the SPLIT exception below can re-include — git cannot
# re-include children of an excluded directory. /split run reports are committed
# evidence (commands/split.md 측정 고지 1번이 인용한다), unlike the other generated reports.
reports/*
!reports/SPLIT/
**/_design/
**/_analysis/
**/_research/
**/_proposals/
*-design-spec.md
*-analysis-report.md

# Session handoffs — LOCAL-ONLY since 2026-08-17 (user decision via /save).
# Previously committed for cross-machine visibility, but the landing-flow gate
# makes per-session commits impractical (five sessions of permanent dirty tree).
# Trade-off accepted: handoffs are per-machine local records; cross-machine
# context restore relies on git-tracked guides + auto-memory instead.
.artibot/HANDOFF.md
.artibot/handoffs/
# /split run state (plan.json, run.json, list-agents.txt, locks/, per-limb brief.md in
# worktrees) — per-machine local records, never committed (commands/split.md "plan" step 7).
.artibot/split/
.artibot/state.yaml

# Session work products written at the repo root (reports, proposals, exported
# docs). Same local-only rationale as the handoffs above: they are per-session
# records, not repo state. Root-anchored so it cannot swallow a future
# `plugins/*/artifacts/` that a build step legitimately tracks.
# Concrete reason it is here: during the v4.49.0 release this directory sat
# untracked and un-ignored, one `git add -A` away from shipping a .docx.
/artifacts/

# Auto-generated per-session timeline log — hooks rewrite it every session,
# so it only adds dirty-tree noise to git status / /save. Read from the working
# tree by hooks (session-notes.js, dev-verify-gate.js, pr-description-builder.js);
# no consumer requires it to be git-tracked. Not cross-machine state (unlike HANDOFF.md).
.artibot/SESSION-NOTES.md

# Ambient Conversation Ledger — per-session denoised conversation archive.
# Local-only (DATA POLICY): contains verbatim user/assistant conversation text
# (secret-redacted, but still private). NEVER commit — keep out of git entirely.
# `**/` so it matches whether the hook's projectRoot is the repo root or a
# nested cwd (e.g. plugins/artibot/.artibot/ledger/ during in-repo dev).
**/.artibot/ledger/

# ── v5 정본/로컬 경계 (설계 §"gitignore 경계", ARTIBOT-5.0-DESIGN.md:161) ─────
# 추적(정본, 규칙 없음 — 여기 나열하지 않는다):
#   ARTIBOT.md · .artibot/project.md · .artibot/missions/ · .artibot/adr/
#   .artibot/memory/ · .artibot/guides/**(설계 세트)
# 로컬(재생성 가능하거나 사적 데이터 — 아래 4규칙):
#
# `**/` 는 위 ledger 규칙(:111)과 같은 이유다. 훅의 projectRoot 가 리포 루트가
# 아니라 중첩 cwd 로 잡히는 경우가 실제로 있다 — 그 증거가
# `plugins/artibot/scripts/hooks/.artibot/ledger/` 다. runtime/state 도 같은
# projectRoot 로 쓰이므로 같은 폭이 필요하다.
#
# 디렉터리형(`dir/`)이지 `dir/**` 가 아닌 것은 의도다. git 은 제외된 디렉터리의
# 자식을 negation 으로 되살릴 수 없으므로, 이 형태는 나중에 누군가 `!.../x.md`
# 를 넣어 사적 데이터를 되살리는 일을 구조적으로 막는다(fail-closed).
# 대가: 이 디렉터리들에는 `.gitkeep` 을 추적시킬 수 없다. 런타임 코드가 쓰기
# 시점에 mkdir 하므로 필요 없다.
#
# 원장·이벤트·evidence 등 런타임 산출물. ledger 는 `.artibot/ledger/` 에서
# `.artibot/runtime/` 으로 옮겨갈 예정이나 개명은 이번 범위 밖이라 위 :111
# 규칙을 그대로 둔다 — 두 규칙이 당분간 공존한다.
**/.artibot/runtime/
# 세션 전사 원문. DATA POLICY: 사용자/어시스턴트 대화 원문을 담는다. 절대 커밋 금지.
**/.artibot/transcripts/
# Generated Project Knowledge — `generated:true canonical:false` 프론트매터를
# 단 재생성 산출물(architecture·conventions·known-failures·active-initiatives).
# 정본이 아니라 HANDOFF 렌더와 같은 등급이다(설계 :474).
**/.artibot/generated/
# 정본 상태의 투영(projection). 재생성 가능하므로 로컬 — 결정 B1.
**/.artibot/state.yaml

# /scorecard snapshot store — per-machine progress history appended by
# lib/planning/scorecard.js. Same local-only rationale as SESSION-NOTES.md:
# it is a record of what one operator measured in one session, not repo state,
# and it grows on every /scorecard run. Named as a single file rather than
# `.artibot/` so it cannot swallow the seven tracked files in that directory
# (guides/, failure-patterns.json, the backlog notes).
.artibot/scorecard.json

# Claude Code plan-mode ephemeral state — machine-local absolute paths, stale
# across sessions. Never commit.
.plan-state.json
