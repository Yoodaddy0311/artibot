# Artibot Claude 4.7 업데이트 안내 (2026-04-20)

> 비개발자도 이해할 수 있도록 쉬운 말로 정리한 업데이트 리포트입니다.

## 한 줄 요약

**"Claude가 더 똑똑한 4.7 버전으로 업그레이드됐어요. Artibot이 이 새 Claude를 잘 활용하도록 몸에 맞는 옷으로 바꿔 입혔습니다."**

---

## 오늘 무엇을 했나요?

### 🎯 배경

Anthropic이 **Claude 4.7 버전**을 새로 공개했습니다. 쉽게 말하면 Artibot의 "두뇌"가 더 좋아진 셈이에요. 그런데 새 두뇌는 예전 두뇌와 사용법이 조금 달라서, 몇 가지를 **그대로 두면 오작동**합니다.

예를 들면:
- 예전 Claude에게 "말투를 조정해줘"라고 요청하는 방식이 **새 Claude에서는 오류**를 냅니다
- 새 Claude는 **혼자서 조용히** 일하려는 성향이 강해서, Artibot의 핵심 강점인 "팀으로 병렬 처리" 기능이 약해질 수 있습니다
- 새 Claude는 같은 글을 **최대 35% 더 많은 토큰**(=계산 단위)으로 읽기 때문에, 한도 기준을 낮춰야 합니다

그래서 **Artibot을 새 Claude에 맞게 대대적으로 재정비**했습니다.

### 📋 진행 방식

1. **P0 (긴급 수정)**: 그대로 두면 오류 나는 것부터 고치기
2. **P1 (신기능 채택)**: 새 Claude가 제공하는 편리한 기능들 흡수
3. **P2 (장기 효율)**: 1M 컨텍스트, 고해상도 이미지 등 큰 그릇 활용
4. **검증 + 커밋**: 자동 테스트 5,183개 통과 → 저장·공유

---

## 🎁 주요 변경 사항 (사용자 관점)

### 1. 🚦 "커맨드별 노력 단계" 자동 배정 — AI가 상황에 맞는 집중도로 일함

**무엇인가요?**

새 Claude 4.7에는 **`effort`(노력)** 라는 다이얼이 생겼습니다. 같은 일도 "대충 빨리" vs "온 힘을 다해" 중에서 선택할 수 있어요.

Artibot은 이제 유저가 뭘 요청하냐에 따라 **자동으로 다이얼을 돌립니다**:

| 노력 단계 | 언제 쓰나요? | 왜? |
|---|---|---|
| 🔥 **xhigh** (최대) | 코드 작성, 팀 작업, 테스트 작성 | 중요한 작업은 최고 품질 |
| ⚡ **high** (강) | 코드 리뷰, 계획 세우기, 문제 분석 | 꼼꼼한 판단 필요 |
| ✅ **medium** (중) | 일일 회고, 설명, 문서 작성 | 균형 있게 빠르게 |
| 💨 **low** (약) | 상태 확인, 업데이트, 간단한 응답 | 돈·시간 절약 |

**사용자가 할 일**: 없습니다. AI가 알아서 판단해요.

### 2. 💰 "예산제" 도입 — AI가 폭주하지 않도록 상한선 설정

**무엇인가요?**

가끔 AI가 너무 오래 혼자 고민하거나, 같은 작업을 계속 반복할 때가 있어요. 이제 **"이 작업에 쓸 토큰은 최대 N개야"** 하고 모델에 **미리 알려줄 수 있습니다**.

예시로, 팀 작업(`/team`)이나 구현 작업(`/implement`)에는 다음 권장 예산을 설정합니다:

| 상황 | 권장 예산 | 설명 |
|---|---|---|
| 대형 리팩토링 | 128,000 토큰 | "넉넉하게, 철저하게" |
| 일반 팀 구현 | 64,000 토큰 | 기본값 |
| 짧은 배치 | 20,000 토큰 | 최소값 |
| 개방형 탐색 | **설정 안 함** | 제한하면 AI가 포기할 수 있음 |

**사용자가 할 일**: 없습니다. 기본값이 자동 적용됩니다.

### 3. 🧠 "팀 병렬 처리" 강제 — Claude가 혼자 일하려고 할 때 막기

**무엇인가요?**

새 Claude 4.7은 "**혼자서 다 처리하자**"는 성향이 강해졌습니다. 그런데 Artibot의 핵심 강점은 **"여러 명의 전문가가 동시에 병렬로 일하는 것"**(Operator-Waits DNA) 이에요.

이 두 가지가 충돌하면 Artibot의 강점이 약해지므로, **CLAUDE.md 규칙 문서에 다음 문구를 명시적으로 추가**했습니다:

> "Claude 4.7이 서브에이전트 수를 줄이려 해도, Artibot은 이 기본값을 **명시적으로 무시**한다. 2개 이상의 독립 작업이 감지되면 유저가 `/team`을 입력하지 않아도 **반드시** 팀 병렬 모드로 전환한다."

**효과**:
- 유저가 "테스트도 작성하고 문서도 업데이트해줘"라고 하면 **자동으로** 두 명의 전문가가 동시에 일함
- 유저가 슬래시 명령어를 몰라도 AI가 **상황을 감지해 자동 발동**
- 이 규칙이 **모든 커맨드**(/implement, /plan, /code-review, /verify, /daily 등)에 적용됨

### 4. 📐 "토큰 한도" 버퍼 조정 — 새 Claude의 식탐에 대비

**무엇인가요?**

새 Claude 4.7은 같은 글을 읽어도 **최대 35% 더 많은 토큰**을 소비합니다 (새로운 글자 단위 분석 방식 때문). 기존 임계값을 그대로 쓰면 한도 초과로 작업이 끊길 수 있어요.

| 설정 | 이전 | 지금 | 효과 |
|---|:---:|:---:|---|
| 토큰 효율 모드 발동 | 75% | **60%** | 더 일찍 절약 모드 진입 |
| 컨텍스트 압축 발동 | 75% | **70%** | 더 일찍 청소 시작 |

**사용자가 할 일**: 없습니다. 자동으로 작동합니다.

### 5. 🖼️ 고해상도 스크린샷 지원 — UI 검증이 훨씬 정확해짐

**무엇인가요?**

새 Claude 4.7은 **훨씬 큰 이미지**를 읽을 수 있습니다:

| 항목 | 이전 | 지금 |
|---|:---:|:---:|
| 최대 너비 | 1,568 픽셀 | **2,576 픽셀** |
| 최대 해상도 | 1.15 메가픽셀 | **3.75 메가픽셀** |
| 좌표 매핑 | 스케일 계산 필요 | **1:1 정확 매핑** |

**효과**:
- `/visual-check`로 화면 비교할 때 원본 해상도 그대로 분석
- Claude가 "여기 x=400, y=200 위치 버튼이 깨져있어요"라고 할 때 **실제 픽셀과 정확히 일치**
- 디자인 검증이 훨씬 정확해짐

### 6. 📚 "1M 컨텍스트" 전략 — 큰 프로젝트 전체를 한 번에 볼 수 있음

**무엇인가요?**

새 Claude 4.7은 **100만 토큰**의 초대형 컨텍스트 윈도우를 **추가 비용 없이** 표준가로 제공합니다. 대략 **중간 규모 프로젝트의 모든 코드를 한 번에 로드**할 수 있는 수준이에요.

이제 `/load --full-context` 옵션을 쓰면:
- 프로젝트 전체 파일을 한 번에 메모리에 올림
- AI가 "이 파일과 저 파일을 같이 봐야 알 수 있는" 복잡한 질문에 즉시 답변 가능
- 대신 **500,000 토큰 이하** 프로젝트에만 권장 (그 이상은 부담스러움)

**새 관리 기준**:
- 400k 미만 사용: 청소 안 해도 OK
- 400k~700k: 주의, 중단점에서 저장 권장
- 700k~900k: 즉시 청소 또는 `--full-context` 해제
- 900k 이상: 긴급, 생존 모드

### 7. 🎨 Claude Design 연계 — 프레젠테이션 만들 때 더 똑똑해짐

**무엇인가요?**

Anthropic이 **Claude Design**이라는 실험적 제품을 공개했습니다 (Pro/Max/Team/Enterprise 플랜). 채팅하면서 화면에서 디자인/프로토타입/슬라이드를 만들 수 있어요.

Artibot의 `/ppt`(프레젠테이션 설계)와 **서로 보완 관계**입니다:

| 단계 | 도구 | 역할 |
|---|---|---|
| 1. 시각 시안 만들기 | Claude Design | 레이아웃, 비주얼, 컬러/폰트 |
| 2. 내러티브 구조 짜기 | Artibot `/ppt` | 슬라이드 순서, 스피커 노트, 스토리 아크 |
| 3. 통합 | PPTX 내보내기 ↔ Artibot 구조 적용 | 최종 발표 자료 |

**⚠️ 주의**: **Pencil MCP** (`.pen` 파일)은 Claude Design과 **전혀 다른 3rd-party 도구**예요. 혼동하지 마세요.

---

## 🔧 기술 수정 (요약)

이 섹션은 개발자를 위한 요약이며, 일반 사용자는 건너뛰어도 됩니다.

| 구분 | 변경 |
|---|---|
| 🚨 Breaking fix | `temperature`/`top_p`/`top_k` 파라미터를 **기본값만 써야 함** (4.7에서 다른 값 주면 오류) — 규칙 반전 |
| 🆔 모델 ID 갱신 | 복잡한 추론 작업 기본 모델 → `claude-opus-4-7` (Sonnet은 `4-6` 유지) |
| 🆕 코드 추가 | `lib/cognitive/router.js`에 `EFFORT_POLICY` + `getEffortForCommand()` export |
| 🆕 상수 추가 | `lib/visual/visual-validator.js`에 `HIGH_RES_DEFAULT` (2576px/3.75MP) |
| 📝 문서 추가 | `commands/team.md`·`implement.md`에 Task Budget 가이드 |
| 🧪 테스트 추가 | **19개 신규 테스트** (커맨드별 effort 자동 배정 로직 100% 커버) |

---

## ✅ 검증 결과

| 항목 | 결과 |
|---|:---:|
| ESLint (문법 검사) | 0 오류 |
| Vitest (자동 테스트) | **5,183개 전부 통과** (이전 5,164개 → +19 신규) |
| 회귀 (기존 기능 깨짐) | 없음 |
| 코드 리뷰 (2단계) | Phase 4 Cross-check + Phase 4.5 Inspection **모두 APPROVE** |

---

## 🎓 이번 작업에서 배운 것

### Git 머지 전략 사고와 교훈

이번 작업을 원격 저장소에 올리는 과정에서 **한 번 실수**가 있었습니다.

**무슨 일이 있었나?**
- 원격에 다른 세션의 자동 저장 커밋이 50개 이상 쌓여있어서 단순 푸시가 거부됨
- `git pull -X theirs`라는 "모든 충돌을 원격 우선으로 해결" 전략 사용
- 이게 **런타임 파일(자동 생성물)은 잘 처리했지만, 내가 의도적으로 수정한 3개 파일도 원복**시켜버림
- 즉시 탐지해 복구 커밋(`30080cb`)으로 되살림

**교훈**:
- `-X theirs` / `-X ours` 같은 "전체 전략"은 **의도적 변경과 자동 파일을 구분 못 함**
- 앞으로는 **파일 단위로** `git checkout --theirs <파일>` 식으로 선별해야 함
- 이 교훈을 Artibot의 `git-conflict` 스킬에 **영구 기록** → 다음부터 AI가 자동으로 올바른 전략 선택

**실전 검증**: 이 교훈 문서화 커밋을 푸시할 때 또 다른 분기 발생. 이번엔 문서화한 새 전략을 따라 **1줄 충돌만 수동 해결**해 성공. 배운 걸 바로 써먹은 셈이에요.

---

## 📦 변경된 파일 (총 20개)

- 규칙/에이전트: 3개
- 커맨드: 5개
- 코어 라이브러리: 2개
- 스킬: 5개
- 문서: 3개 (CLAUDE.md × 2, CHANGELOG.md)
- 테스트: 1개 (신규)
- 릴리즈 노트: 1개 (이 문서)

---

## 🎯 사용자가 기억할 3가지

1. **새 Claude 4.7은 더 똑똑하지만 사용법이 조금 다름** → Artibot이 알아서 맞춤. 사용자는 그대로 쓰면 됨.
2. **슬래시 명령어 외우지 않아도 됨** → "테스트도 해줘, 문서도 써줘" 같이 자연스럽게 요청하면 AI가 자동으로 팀 병렬 모드로 전환
3. **큰 파일·큰 프로젝트도 한 번에** → 1M 컨텍스트 + 고해상도 이미지로 대형 작업 가능

---

**작성자**: Artibot Team (자동 검증 완료)
**버전**: v2.6.0
**이전 버전**: [v2.3.0 업데이트 안내](./RELEASE_NOTES_2.3.0_KO.md)
