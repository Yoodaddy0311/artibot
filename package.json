{
  "name": "artibot-repo",
  "private": true,
  "version": "4.50.0",
  "description": "Artibot monorepo root — see plugins/artibot/ for the published plugin.",
  "license": "BUSL-1.1",
  "engines": {
    "node": ">=20"
  },
  "type": "module",
  "scripts": {
    "//": "Delegate to the workspace that declares the runner — see vitest.config.js for why bare `npx vitest` here is unpinned.",
    "test": "npm --prefix plugins/artibot test",
    "test:coverage": "npm --prefix plugins/artibot run test:coverage",
    "lint": "npm --prefix plugins/artibot run lint",
    "ci": "npm --prefix plugins/artibot run ci"
  }
}
