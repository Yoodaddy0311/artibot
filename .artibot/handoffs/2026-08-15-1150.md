---
machineId: AsusHeechangLee_HeechangLee
createdAt: '2026-08-15T02:05:00.000Z'
branch: master
generator: artibot-handoff
schemaVersion: 1
---

# HANDOFF — 2026-08-15 11:05

> 다음 P0: **없음 — 릴리스 완료, 워킹트리 clean.** 남은 것은 §4 의 미해결 4건이고 전부 잠복·저위험이다. 새 세션은 아무 데서나 시작해도 된다.

## 1. 지금 상태

| 항목 | 값 | 측정 |
|---|---|---|
| Branch | `master` @ `f946001a` (**v4.45.0**) | `git rev-parse --short HEAD` |
| 태그 | `v4.45.0` 로컬·원격 반영 | `git ls-remote --tags origin \| grep v4.45.0` |
| Tree | **clean** (mod 0 / staged 0 / untracked 0) | `git status --porcelain` |
| Tests | **9,980 pass / 0 fail / 35 skip (10,015)** · 451 files | `npm test` (cwd=`plugins/artibot`), 리더 10:13:20 |
| Lint | `npm run lint` 0/0 · **`npx eslint .` exit 0** | — |
| Coverage | 89.39 / 80.12 / 91.27 / 91.52 — **`lib/` 한정** | `npm run test:coverage`, health-verifier 09:37 |
| `npm run ci` | **8단계 전건 통과** | 릴리스 전 실행 |
| 설치본 | **4.45.0** (`sync:local` 완료) | `~/.claude/artibot/package.json` |
| Unpushed | 0 | `git rev-list --left-right --count origin/master...HEAD` → `0 0` |

> ⚠️ 루트 `package.json` 에 `scripts` 키 없음. npm 명령의 cwd 는 `plugins/artibot/`.

> ⚠️ `ago.selfControl.autoMacroRegister.enabled = true` 라 `macro-learner#writeMacroEntry` 가 추적 파일 `artibot.config.json` 을 직접 재기록한다. **커밋 전 `git status` 필수.**

### ✅ pre-push 훅이 설치돼 있다 — 이 머신 한정

```
.git/hooks/pre-push   설치됨 · up to date · hash 24036c86…
core.hooksPath        미설정 (복사 방식이므로 정상)
```
**다음 push 부터 10종 게이트가 선다**(~9-11s): structure·agents·skills·commands·hooks·doc-links·md-render·model-policy·readme·lint.
- 드리프트 시: `npm run hooks:check` → STALE 이면 **diff 를 먼저 읽고** `npm run hooks:install`
- 되돌리기: `rm .git/hooks/pre-push` 한 줄
- 우회: `git push --no-verify` 또는 `ARTIBOT_SKIP_PREPUSH=1`(후자는 **stderr 로 경고**를 뿜는다)
- **`.git/` 은 커밋되지 않으므로 다른 머신·클론에는 설치되지 않는다.** 각자 1회 실행 필요

## 2. 이번 세션 한 일

30일 공백 복원 → 보안 감사 → 결함 종결 → **릴리스**. 커밋 5건.

| 커밋 | 내용 |
|---|---|
| `850b5682` | 핸드오프 복원 (실측 3인 병렬) |
| `4d8f1ad0` | **H1**(인스톨러 상호배제 부재) · **M6**(부분설치 fail-open) · **M1**(stdin UTF-8) · **M2**(isMainEntry 심링크) |
| `f9af7905` | MCP stdin 2경로(`fallbackStdioLoop`·`createStdioTransport`) + 인스톨러 주석 인용 6건 교정 |
| `b38275f4` | **M3**(hooksPath 신뢰경계) · **M4**(settings 권한) · **L1~L3** · **`-LiteralPath` 80곳** · **pre-push 실배치** · **lockstep 게이트** · `bin/` 스캔 확장 |
| **`f946001a`** | **release v4.45.0** — lockstep 11곳 + CHANGELOG |

**`/ultrareview` 결과**: CRITICAL 0 · HIGH 1 · MEDIUM 6 · LOW 4 · **깨끗 판정 10건**(null-result 병기).

**게이트 3연속 성공**: `4d8f1ad0`·`f9af7905`·`b38275f4`·`f946001a` 전부 `ci/**` → CI 그린 → `--ff-only` 로 착지했고 **`Bypassed rule violations` 가 한 번도 안 나왔다.** 같은 세션의 직푸시(`850b5682`)에서는 나왔다.

## 3. 의도/현재 가설

이 리포의 결함은 **"작동한다고 믿던 것이 실제로는 작동하지 않는다"** 형태로 나온다. 이번 세션이 그걸 다섯 번 확인했다 — 락이 없던 인스톨러, 성공을 보고하던 부분 설치, 배치되지 않은 훅, 이미 해결된 디렉터리만 감시하던 스캔, 거부당하는 명령을 안내하던 문서.

**그리고 이번엔 반대 방향도 확인했다**: 만든 게이트를 실제로 통과시켜 봤다. `pre-push` 가 **자기 자신의 소스 변경을 포함한 커밋을 4회 검문하고 통과**시켰다.

**현재 가설**: 남은 결함은 **클래스의 꼬리**다. 이번에 세 클래스가 각각 4→14→76→80, `readPayload`→2경로, `scripts/hooks/`→`bin/` 로 확장됐고, **매번 "끝났다" 고 판단한 지점에서 한 겹이 더 나왔다.** 개별 열거가 아니라 **분류·census** 로 전환했을 때만 닫혔다.

## 4. 즉시 진행할 일

**P0 없음.** 릴리스가 나갔고 트리는 clean 이다. 아래는 전부 저위험·잠복이다.

| # | 항목 | 근거 | 확신도 |
|:--:|---|---|---|
| 1 | **`_dispatcher-utils.js#mergeResults` 의 `__proto__` 병합 방어 부재** — 자식 stdout 의 `{"__proto__":{…}}` 가 병합 봉투 프로토타입을 교체. 전역 오염 아니고 `JSON.stringify` 가 own prop 만 내보내 **현재 출력은 깨끗**. 잠복 | 방어코드 grep 0건 | 실측 |
| 2 | **인라인 `argv[1]` 가드 34곳** (`bin/`·`scripts/hooks/` 밖). `scripts/ci/` 3건은 **v4.43.0 퍼센트인코딩 형태 그대로**, 2건은 `basename` 비교로 **false-POSITIVE** | 센서스 37곳 중 스캔범위 밖 | 형태 실측 / 영향 미확인 |
| 3 | `bin/artibot-dashboard.mjs` 동일 결함 1곳 — `KNOWN_INLINE_GUARD_GAPS` 에 기재, **고치는 순간 staleness 테스트가 red** (자기만료) | `main-entry.test.js:163-165`·`:180-191` | 실측 |
| 4 | `bin/artibot.js:229` 가드 없는 `main().catch(` — **오늘은 결함 아님**(import 하는 소스 0). 전제가 바뀌면 결함. 게이트 §9 에 기재 | `main-entry.test.js:114`·`:244` | 실측 |
| 5 | `eslint.config.js` 의 `js.configs.recommended` 에 `files` 키 없음 → 6개 디렉터리 밖 `.js/.mjs` 는 "recommended 는 받되 node 전역은 못 받음". **증상(.ap-boot.mjs)은 ignores 로 닫혔고 구조적 원인만 남음** | `:44` | 실측 |
| 6 | `## [Unreleased]` 섹션이 **2개** — `:1981` 은 HEAD 이전부터 있던 선행 부채이고 거기 적힌 `/adr`·`/migrate` 는 **이미 출하됨** | `git show HEAD~:… \| grep -c` = 2 | 실측 |
| 7 | `out += chunk` 프로덕션 5곳 — `watch-ingest.js` 는 **도달 불가**(소비값이 1줄), 나머지 4곳(`rollback-guard`·cron 3종)은 **경계 도달 여부 미측정** | 교차검수 독립 스캔 | 부분 |
| 8 | ADR-003 LOW-2 · 학습저장소 프로젝트명 오염 · 원장 파편 11파일 | 이전 핸드오프 이월 | 커밋 작성자 측정 |

## 5. 미해결 결정/질문

**없음.** 이번 세션의 결정은 전부 내려졌다 — O-2 는 ⓐ(공유 import), S-2 는 `umask 077` 서브셸, `-LiteralPath` 는 cmdlet census, released 섹션은 개작 대신 날짜 주기 가필.

## 6. 다음 세션 첫 프롬프트 후보

1. **"남은 인라인 가드 34곳을 정리하자 — `scripts/ci/` 의 퍼센트인코딩 3건부터"**
   → §4 #2. 그중 3건은 v4.43.0 이 고친 것과 **같은 형태가 그대로 남은 것**이고 2건은 false-positive 방향이라 성격이 더 나쁘다.
2. **"`__proto__` 병합 방어를 넣자"**
   → §4 #1. 잠복이지만 3줄이고, 이번 세션에서 유일하게 **배정 자체가 누락된** 항목이다.
3. **"`## [Unreleased]` 중복을 정리하자"**
   → §4 #6. `:1981` 항목은 이미 출하된 기능이라 릴리스 노트가 두 벌 존재하는 상태다.

## 7. 컨텍스트 복원 핵심 파일

- `CONTRIBUTING.md#Landing changes on master` + `#Trust boundary` — 게이트 플로우와 훅 배치의 **정본**
- `plugins/artibot/scripts/git-hooks/install.js` · `pre-push` — 배치·drift 검사
- `plugins/artibot/tests/firewall/workflow-branch-lockstep.test.js` — branches lockstep 게이트 (헤더에 **못 보는 것 5항목** 기재)
- `plugins/artibot/tests/hooks/main-entry.test.js` — 재인라인 스캔 (`BIN_DIR` 포함) + `KNOWN_INLINE_GUARD_GAPS` 자기만료
- `plugins/artibot/tests/dispatcher/dispatcher-payload-utf8.test.js` — **NEGATIVE CONTROL 패턴의 표준**
- `plugins/artibot/install.ps1#Copy-DirAtomic` · `install.sh#atomic_replace_dir` — 락·staging·공백가드
- `plugins/artibot/CHANGELOG.md` `## [4.45.0]` — 이번 릴리스 전문

## 8. 메타

> 생성: 2026-08-15 11:05 · `/team` 4라운드(팀원 8인) + `/ultrareview` + 게이트 착지 4회 + 릴리스 1회
> 커밋 5건 · 테스트 +65 · fail 1→0 · files +7

### 이번 세션의 방법론 교훈 (메모리에도 저장됨)

- **인용 오류 12건 이상.** 전부 "직접 열어보기"로만 잡혔고 **자동 게이트 검출 0건**. 스폰 프롬프트의 *"내 인용이 틀렸으면 그대로 보고하라"* 가 실제 검출 수단이었다. 리더·팀원 양쪽이 틀렸고 서로 교정했다. → `feedback-citation-relay-discipline`
- **§9 픽스처 함정이 실발동.** 198KB 픽스처가 65,536B 를 넘겼는데도 ASCII 가 오프셋을 밀어 대조군이 `fffd=0`. 크기 단언만 있었으면 전 파일이 공허하게 green. → `feedback-fixture-must-reach-failure-region`
- **테스트 하네스 자신이 같은 결함을 가질 수 있다.** `out += chunk` 로 자식 stdout 을 읽던 하네스가 M1 과 동일하게 손상돼, 잘못된 회귀 보고나 단언 약화로 이어질 뻔했다.
- **자기 편집이 자기 인용을 썩힌다.** 순수 주석 추가만으로 `install_hooks` 가 +13줄 밀렸다. → 주석에서는 줄번호보다 **심볼명**.
- **null-result 가 세 번 정답이었다.** `out += chunk` 결함 0건 · 재발방지 게이트 미구축(87% 예외라 부채) · 커버리지 미달 0건.
- **결함 클래스는 "끝났다" 지점에서 한 겹 더 나온다.** 개별 열거가 아니라 **cmdlet 별 `ContainsKey` 실측 · 디렉터리 census** 로 전환했을 때만 닫혔다.
- **문서도 §6(시점 관측 ≠ 상태)의 대상이다.** 작성 시점엔 참이던 CHANGELOG 문안이, 자기가 서술하는 상태를 바꾸는 diff 와 함께 착지하면 거짓이 된다. 최종 검수가 이걸로 착지를 한 번 막았다.

### 미확인 (삭제 없이 전파 — 다음 세션이 이 유보를 지우지 말 것)

- **인스톨러를 한 번도 실행하지 않았다.** H1 재현·락 상호배제·부분실패 종료코드·`-LiteralPath` 전후 대조는 전부 **추출 하네스**. ps1↔sh **라이브 교차 실행 미관측**
- **`-LiteralPath` 80곳 중 전후 대조는 2함수뿐.** 나머지는 "정상 경로에서 `-Path`/`-LiteralPath` 결과 동일"(PS 5.1 실측)에 근거
- Ctrl-C 에서 PowerShell `finally` 가 도는지 **미확인**
- **win32 전용 검증.** 심링크는 `mklink /J` 정션만. Linux/macOS `symlinkSync`·BSD `stat -f %Lp` 미실행
- **커버리지는 `lib/` 한정** — `bin/`·`scripts/` 는 coverage include 밖이라 **측정 자체가 불가**
- `lib/mcp/server.js` 실물 미구동(목 주입만). `t.write` 경로·백프레셔 미검증
- **훅의 POSIX·linked worktree·리포 하위 디렉터리 push 동작 미확인**
- `install.sh` node 분기의 `fs.chmodSync` 는 WSL 에 node 가 없어 **실행 검증 못 함**(셸 등가물만 실측)
- 최종 검수는 **테스트를 한 건도 실행하지 않았다**(코드 재검 범위 밖)
- `git fetch` 는 착지 직후들만 — 그 이후 원격 변화 미확인
