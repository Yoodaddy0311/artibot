---
machineId: AsusHeechangLee_HeechangLee
createdAt: '2026-08-14T14:02:00.000Z'
branch: master
generator: artibot-handoff
schemaVersion: 1
---

# HANDOFF — 2026-08-14 23:02

> 다음 P0: 사이드 브랜치 게이트 플로우 **실사용 채택 판단** — 게이트 인프라는 v4.44.0에 들어갔으나 원격 `ci/**` 브랜치 0건으로 한 번도 쓰이지 않았다. 직푸시는 구조적으로 전건 bypass 된다.

## 1. 지금 상태

| 항목 | 값 | 측정 |
|---|---|---|
| Branch | `master` @ `efb4e054` (v4.44.0) | `git rev-parse --short HEAD` |
| Tree | mod 0 / staged 0 / untracked 0 — **저장 직전 기준** (아래 주의) | `git status --porcelain` (빈 출력) |
| WIP | 31 / 955 (3.25%) — **전부 push 완료, 미푸시 0** | `git log --pretty=%s HEAD \| grep -Eci '^(\[wip\]\|wip\b\|wip:\|wip\()'` |
| Tests | **9,915 pass / 1 fail / 35 skip (9,951)** · 444파일 · 30.66s | `npm test` (cwd=`plugins/artibot`), 리더 직접 실행 23:00:13 |
| Lint | error 0 / warning 0 · exit 0 | `npm run lint` |
| Typecheck | **N/A — 이 리포에 해당 없음** (tsconfig 0건, `.ts` 0건, typescript 미설치) | `find . -name "tsconfig*.json"` |
| Build | **N/A — 스크립트 미정의** | `plugins/artibot/package.json` |
| Unpushed | 0 | `git rev-list --left-right --count origin/master...HEAD` → `0 0` |

> ⚠️ **루트 `package.json`에는 `scripts` 키가 없다.** 모든 npm 명령의 cwd 는 `plugins/artibot/` 이다. 루트에서 `npm test` 는 실패한다.

> ⚠️ **위 Tree 측정은 이 핸드오프를 저장하기 *직전* 값이다.** 저장 직후에는 `/save` 자신의 산출물 3건이 정상적으로 나타난다 — `M .artibot/HANDOFF.md`, `?? .artibot/handoffs/2026-08-14-2303.md`, `D .artibot/handoffs/2026-06-08-0746.md`(keep=30 회전으로 mtime 최고령 evict). **이 3건은 결함이 아니며 아래 `autoMacroRegister` 경고와도 무관하다.** 회전은 파일명이 아니라 **mtime 정렬**(`handoff-store.js:248`) 기준이라, OneDrive 재동기화로 mtime 이 바뀌면 evict 순서가 파일명 순서와 달라질 수 있다.

### Git 동기화 상태

| 점검 항목 | 상태 |
|---|---|
| 커밋 안 된 변경 | 아니오 |
| 미푸시 커밋 (ahead) | 0 |
| pull 필요 (behind) | 0 |
| upstream 추적 | 있음 |
| GitHub 최신성 | 최신 (단 `git fetch` 미실행 — 로컬 remote-tracking ref 기준) |
| 다른 머신 미동기화 의심 | 아니오 |

> ✅ 커밋·푸시 동기화 정상 — 유실 위험 없음

### ⚠️ 커밋 전 필수 확인

`ago.selfControl.autoMacroRegister.enabled = true` (`artibot.config.json:1091`) 이 **라이브**다.
`lib/learning/macro-learner.js:486 writeMacroEntry` 가 **추적 파일 `artibot.config.json` 에 직접 쓴다.**
2026-08-14 세션 중 실제로 발생했다(`createdAt` → `2026-08-14T13:53:06.497Z`, 전체 재직렬화로 39+/10−).
**커밋 전 `git status` 를 반드시 볼 것.** 무의미한 diff 가 계속 생긴다.

## 2. 이번 세션 한 일

**세션 성격**: 30일 공백 복원 세션. 코드 변경 0건 — 전부 실측·검증.

- `/resume` — 2026-07-15 핸드오프(30일 전) 복원. stale 판정 후 `/team` 병렬 3인 실측으로 갱신
- **A · WIP/squash 판정** → NOT-RECOMMEND 확정 (아래 §5)
- **B · `ae3784e..HEAD` 30커밋 요약** → v4.38.0 → v4.44.0 복원 (아래)
- **C · 건강도 실측** → 테스트 9,915/9,951, 실패 1건 원인 규명 (아래 §5)
- 리더가 A·B·C 전 수치를 **독립 재측정**해 일치 확인. 팀원이 리더 전제 5건을 교정했고 **전부 팀원이 옳았다**

### v4.38.0 → v4.44.0 (30커밋)

| 릴리스 | 커밋 | 무엇이 왜 |
|---|:--:|---|
| v4.39.0 `fdb9315f` | 5 | ADR-003 Stage1 — autopilot EXECUTE 러너 pluggable(`--runner dynamic`). 미지정 시 기존과 바이트 동일. "workflow" 1단어가 6대상 지칭 → Canonical Naming 고정 |
| v4.40.0 `c4bb814e` | 2 | Stage2 — `autopilot.runner.autoSelect` 옵트인. **기본 OFF 출고**, 명시 `--runner` 최우선 |
| v4.41.0 `bc8dcae2` | 1 | 모델 단일티어화 — `fable.enabled=false` kill-switch, frontmatter 20종 fable→opus(28/28), catalog `claude-opus-5` |
| v4.42.0 `eb82a885` | 1 | 학습 저장소 모델 귀속 라벨 0건 → transcript `assistant.message.model` 을 진실원으로. 서브에이전트 턴은 형제 파일에 있어 메인만 읽으면 누락(1,225 중 1,114 = 91%) |
| v4.43.0 `9455320a` | 11 | **훅 신뢰성.** direct-run 가드 34건 전수 / `isMainEntry` 퍼센트인코딩으로 비ASCII 경로에서 디스패처 5개 조용히 사망 / 채점기가 3개월간 2종 상수만 출력 / `zero-result-guard` 발화율 0% / `clearCache` 가 마켓플레이스 전체 rmSync (2026-08-10 캐시 소실 근본원인) |
| v4.44.0 `efb4e054` | 10 | **중계 계약** 4파일 + parity 테스트 / 인스톨러 노출창 54,640ms → 161ms / install.ps1 terminating IOException BLOCKER / `resolveProjectRoot` 단일화 / 사이드 브랜치 게이트 |

## 3. 의도/현재 가설

이 30커밋의 축은 기능 추가가 아니라 **"작동한다고 믿던 것이 실제로는 작동하지 않았다"의 연쇄 발굴**이다 — 발화율 0%인 가드, 3개월간 상수만 찍던 채점기, 인코딩 탓에 죽던 디스패처 5개, 전건 bypass 되던 브랜치 보호.

**현재 가설**: 같은 패턴이 아직 남아 있다. v4.44.0이 만든 사이드 브랜치 게이트도 **아직 한 번도 쓰이지 않았다**(원격 `ci/**` 0건) — 게이트를 만든 것과 게이트가 작동하는 것은 다른 진술이다. 다음 세션의 최우선은 "만든 게이트를 실제로 통과시켜 보는 것"이다.

## 4. 즉시 진행할 일

| # | 항목 | 근거 | 확신도 |
|:--:|---|---|---|
| 1 | **사이드 브랜치 플로우 실사용 채택 판단.** 직푸시는 전건 bypass(`CONTRIBUTING.md:266-302`) — `ci/**` → SHA 그린 확인 → `git merge --ff-only` 를 실제로 써야 게이트가 의미를 가진다 | `1f151675`; 원격 `ci/**` 0건 (리더 확인) | 원격상태 실측 / 채택여부 미확인 |
| 2 | **`ci.yml` ↔ `plugin-validate.yml` branches 목록 lockstep 이 수동 불변식.** 목록 밖 브랜치는 check run 자체가 안 생겨 ff-merge 도 bypass. 게이트 없이 산문 경고만 | 두 워크플로 파일 | 실측 |
| 3 | **`install-atomic-replace.test.js:203` 로컬 영구 red.** 이 PC에서 `npm run ci` 가 항상 실패 → `npm run release` 체인이 막힌다. 하네스에 BOM 추가로 해결 가능(대조실험 확인) | 아래 §5 | 실측 |
| 4 | ADR-003 **LOW-2 미해소** — `options.recommendedRunner` 세팅 JS 부재, 프롬프트 주입에만 의존 | `docs/adr/ADR-003-*.md:77` | 실측(문서 이월) |
| 5 | 학습 저장소 **프로젝트명 오염 미정리.** `ads_Mcp 464 / v2 192 / Artibot 137 / artibot 84`. 권고는 `artibot`(84)→`Artibot`(137) 병합 | `77b5f4d6` 본문 | 커밋 작성자 측정, 미재측정 |
| 6 | 원장 파편 11파일 미처리 — 고유 내용 2건 17줄 | `53ece7a7` 본문 | 커밋 작성자 측정 |
| 7 | `score-health` DEGENERATE 보고에 대응 커밋이 30커밋 범위 내 부재 | `825bf6be` 본문 | **추론** |
| 8 | README 카운트 드리프트 — 30커밋에서만 3회 재발, 매번 push 후 CI red 로 발견 | `e8e87ce5`·`1be0246a`·`46546dc5` | 실측 |

## 5. 미해결 결정/질문

### ✅ 해소됨 — WIP/squash (이전 핸드오프의 유일 미해결 항목)

**판정: NOT-RECOMMEND. 이 항목은 종결한다.**
이전 핸드오프의 "31 WIP → `/squash` 권장"은 **stale 이 아니라 무의미해진** 항목이었다. count 31 은 지금도 정확하다.

- WIP 31건 **전부 이미 origin/master 에 푸시됨** (미푸시 0). squash 의 목적(push 전 정리)은 시효 만료
- HEAD 최상단 **연속 WIP 런 = 0** (최신 WIP `a6dc37a6` 2026-06-11 위로 비-WIP 155커밋)
- 실행 시 `abort-mixed` → **exit 1**. no-op(exit 2) 아님. 경로: 신호 `squash-wip.mjs:105-121` → status `:189-195` → `exitCodeFor` `:254-257`(ok/dry-run→0, nothing-to-do→2, **나머지 전부 1**) → `process.exit` `:298`. dry-run 실측 확인
- `--from` 우회 시 published history rewrite → `--force-with-lease` 강제 + 협업자 리베이스 강요
- 신규 유입은 `git.autopilot.commitStrategy = "semantic"` 로 이미 차단 — 2026-06-11 이후 증가 0

### ⚠️ 미해결 — 테스트 1건 영구 실패 (환경)

`plugins/artibot/tests/scripts/install-atomic-replace.test.js:203` — 4/4 결정적, 플레이크 아님.
**프로덕션 코드 결함 아님.** 테스트가 probe `.ps1` 을 BOM 없는 UTF-8 로 쓰는데 PowerShell 5.1 이 ANSI CP949 로 읽어 `바탕 화면` 이 깨짐 → stdout `''`.
BOM(`﻿`) 한 글자 추가 대조실험에서 `SURVIVED=True` 등 5개 단언 전부 충족 → `install.ps1#Copy-DirAtomic` 잠긴-목적지 폴백은 **정상 작동**.
메모리의 "Korean-path collect SyntaxError"와는 **다른 증상**(collect 정상, assertion 실패). 같은 뿌리·다른 발현.
→ **결정 필요**: 하네스에 BOM 추가할 것인가, 아니면 로컬 red 를 감수할 것인가.

### ⚠️ 미해결 — 매크로 스토어 divergence (2026-08-14 세션 중 발생)

세션 중 `macro-learner` 가 config 를 재기록했고, 팀원이 clean 복원을 위해 `git checkout -- artibot.config.json` 로 원복했다.
`runtime/macro-suggestions.json` 은 **git 미추적**이라 원복 대상이 아니었다 → 상태 어긋남:

| 대상 | 값 |
|---|---|
| config `macros.…suggestionId` | `macro-1778732940625` (5월 원본, 원복됨) |
| store approved | `macro-1776731981993` (승인 기록만 남음) |

**재등록되지 않는다.** 라이브인 것은 `autoMacroRegister.enabled=true` 의 **자동 경로**이고, 그 경로가 두 겹으로 막는다:
- `sweepAutoRegister`(`macro-learner.js:586`) — `store.suggestions.filter((s) => s?.status === 'pending')` 로 approved 건이 스윕 대상에서 제외
- `observePrompt`(`macro-learner.js:217-220`) — 같은 fingerprint 가 approved/rejected 면 early-return 이라 새 pending 도 안 생김

> ⚠️ 수동 승인 경로의 `resolveSuggestionTerminalState`(`:268-273`)·`validateApprovalPreconditions`(`:257-263`)와 혼동하지 말 것. `tryAutoRegister`(`:521-563`)는 `resolveSuggestionTerminalState` 를 **호출하지 않는다**(grep 0건). 2026-08-14 검수에서 이 오인이 2회 연속 발생했다.
**영향도 낮음** — 매크로 이름·내용은 config 에 그대로 있고 갱신 등록 1회분만 유실. 기능 파손 아님.
→ **결정 필요**: 스토어의 해당 approved 항목을 pending 으로 되돌릴 것인가, 그냥 둘 것인가.

## 6. 다음 세션 첫 프롬프트 후보

1. **"사이드 브랜치 게이트 플로우를 실제로 한 번 돌려보자 — `ci/**` 브랜치 만들어서 ff-only 머지까지"**
   → §4 #1. 게이트를 만들어 놓고 아직 안 썼다. 가장 큰 미검증 구멍이다.
2. **"`install-atomic-replace.test.js` 하네스에 BOM 추가해서 로컬 `npm run ci` 를 그린으로 만들자"**
   → §4 #3. 이게 red 인 한 로컬 릴리스 체인이 막힌다. 원인·해법 모두 확정됐고 작업량이 작다.
3. **"`ci.yml` ↔ `plugin-validate.yml` branches lockstep 게이트를 만들자"**
   → §4 #2. 수동 불변식은 언젠가 깨진다. 다만 #1 을 먼저 해야 무엇을 게이트할지가 정해진다.

## 7. 컨텍스트 복원 핵심 파일

- `CONTRIBUTING.md:266-302` — 브랜치 보호 bypass 구조 + `ci/**` ff-only 플로우 (이번 세션 P0 의 근거)
- `plugins/artibot/CHANGELOG.md:25-90` — v4.44.0 사이드 브랜치 게이트 상세
- `plugins/artibot/scripts/squash-wip.mjs:105-122` — WIP 연속런/abort-mixed 로직
- `plugins/artibot/lib/learning/macro-learner.js:217-220,586,486` — 자동경로 pending 필터(`sweepAutoRegister`) + fingerprint early-return(`observePrompt`) + 추적 파일 기록(`writeMacroEntry`)
- `plugins/artibot/artibot.config.json:1091` — `autoMacroRegister` 라이브 플래그
- `plugins/artibot/package.json:17-45` — 실제 npm scripts (루트 아님)
- `plugins/artibot/tests/scripts/install-atomic-replace.test.js:203` — 유일 실패 테스트

## 8. 메타

> 생성: 2026-08-14 23:02 · `/team` 병렬 3인(wip-analyst · commit-historian · health-verifier) + 리더 독립 재측정
> 코드 변경 0건 — 전부 실측 세션. 모든 수치에 측정 명령·측정자·시각 명기.

### 미확인 (삭제 없이 전파 — 다음 세션이 이 유보를 지우지 말 것)

- `git fetch` 미실행. 동기 `0/0` 은 **로컬 remote-tracking ref 기준**이며 실제 원격 서버 상태와 다를 수 있다
- **HEAD 의 현재 CI 상태 미조회** (`gh` 미사용). `1f151675`·`efb4e054` 가 `ci/**` 경유로 랜딩했는지도 미확인
- master 외 브랜치의 WIP 분포 미측정 — WIP 수치는 전부 HEAD 도달 범위 한정
- `squash-wip.mjs --from <ref>` 경로는 코드 독해 기반 추론, 미실행
- §4 #5·#6 수치는 **커밋 작성자 측정치**이며 이번 세션에서 재측정하지 않음
- **커버리지 미측정** — 게이트 85/76/85/85 충족 여부 미확인
- `npm run ci` 전체·`validate:bin`·`validate:install`·`eval:runtime:check`·`release:check` 미실행
- ASCII 경로/CI 에서 `install-atomic-replace` 실제 통과 여부 미확인 (CI 로그 미조회)
- config 를 재기록한 **발동 주체**(테스트 vs 세션 훅) 구분 불가
- **비-Windows 동작 전량 미측정** — `realpathSync.native`·8.3 단축경로 등은 Windows 에서만 실측
- 30커밋 중 hunk 단위로 diff 를 연 것은 3건 (stat 12건, 커밋 본문 30건 전건)
