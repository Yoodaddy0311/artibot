---
machineId: AsusHeechangLee_HeechangLee
createdAt: '2026-08-15T00:15:00.000Z'
branch: master
generator: artibot-handoff
schemaVersion: 1
---

# HANDOFF — 2026-08-15 09:15

> 다음 P0: **워킹트리에 미커밋 변경 4파일** — S-3·O-1·주석정리·신규테스트가 검증까지 끝난 채 착지 대기 중. 게이트 플로우(`ci/**` → CI 그린 → `--ff-only`)로 올리면 된다.

## 1. 지금 상태

| 항목 | 값 | 측정 |
|---|---|---|
| Branch | `master` @ `4d8f1ad0` | `git rev-parse --short HEAD` |
| Tree | **mod 3 / untracked 1 — 아래 §4 참조** | `git status --porcelain` |
| Tests | **9,948 pass / 0 fail / 35 skip (9,983)** · 449파일 · 30.57s | `npm test` (cwd=`plugins/artibot`), 리더 직접 실행 09:11:18 |
| Lint | error 0 / warning 0 · exit 0 | `npm run lint` |
| Typecheck / Build | **N/A** (tsconfig 0건 · `.ts` 0건 · typescript 미설치 · build 스크립트 없음) | — |
| Unpushed | 0 (커밋 기준) | `git rev-list --left-right --count origin/master...HEAD` → `0 0` |

> ⚠️ 루트 `package.json` 에 `scripts` 키 없음. 모든 npm 명령의 cwd 는 `plugins/artibot/`.

> ⚠️ `ago.selfControl.autoMacroRegister.enabled = true` 가 라이브라 `macro-learner#writeMacroEntry` 가 추적 파일 `artibot.config.json` 을 직접 재기록한다. **커밋 전 `git status` 필수.**

### Git 동기화 상태

| 점검 항목 | 상태 |
|---|---|
| 커밋 안 된 변경 | **예 — 4파일 (검증 완료, 착지 대기)** |
| 미푸시 커밋 (ahead) | 0 |
| pull 필요 (behind) | 0 |
| upstream 추적 | 있음 |
| 다른 머신 미동기화 의심 | 아니오 |

## 2. 이번 세션 한 일

**세션 성격**: 30일 공백 복원 → 보안 감사 → 결함 수정 → **게이트 첫 실사용 착지**.

| 단계 | 결과 |
|---|---|
| `/resume` | 2026-07-15 핸드오프(30일 전) 복원 → stale 판정 |
| `/team` ① | 실측 3인 병렬 → 핸드오프 재작성 · 커밋 `850b5682` |
| `/ultrareview` | v4.44.0 보안표면 적대 감사 → **HIGH 1 · MEDIUM 6 · LOW 4** (깨끗 판정 10건 병기) |
| `/team` ② | H1·M1·M2·M6 수정 + 회귀 테스트 → 커밋 `4d8f1ad0` |
| **착지** | `ci/**` → CI 6/6 그린 → `--ff-only` → **`Bypassed rule violations` 없이** master |
| `/team` ③ | S-3·O-1·주석정리·회귀테스트·사후검수 (미커밋) |

**착지된 것 (`4d8f1ad0`)**
- **H1** `install.ps1` 에 락이 0건이었다 → sh 와 동일 경로 이식(`New-Item -ItemType Directory` **`-Force` 없이**가 뮤텍스 본체). staging PID 유일화 · prune 축소 · 공백가드에 재귀 카운트 추가. 복제본 실측: OLD 는 10파일 트리를 5파일로 교체하고 **RC=0**
- **M6** `|| true` 가 실패를 삼켜 부분 설치가 `Installation complete!` + exit 0 → 호출부 7곳 `INSTALL_FAILURES` 집계 + `PARTIAL INSTALL` + 비영 종료
- **M1** stdin UTF-8 이 64KB 청크 경계에서 U+FFFD 손상 → Buffer 수집 후 일괄 디코드. `_userprompt-dispatcher` 의 로컬 사본 삭제(그 사본이 버그를 두 곳에 살려둔 원인)
- **M2** `isMainEntry` 가 심링크 경유 실행을 놓침 → fast path 후 realpath 폴백

**미커밋 (검증 완료, §4 참조)**
- **S-3** `artibot-mcp.mjs#fallbackStdioLoop` 에 M1 동일 결함 → `StringDecoder`
- **O-1** `createStdioTransport#lines()` 에 같은 결함(**주 경로**) → 동일 형태 + `decoder.end()`
- **S-1/S-2** 인스톨러 주석의 `install.sh` 줄번호 인용 6건 → **전부 심볼 인용으로 교체**(잔존 0건)
- 신규 `tests/bin/artibot-mcp-stdin-utf8.test.js` 9건

## 3. 의도/현재 가설

`/ultrareview` 가 확인해준 것: **v4.44.0 의 30커밋이 그랬듯 이 리포의 결함은 "작동한다고 믿던 것이 실제로는 작동하지 않는" 형태**로 나온다. 이번에도 그랬다 — `install.ps1` 에 락이 **아예 없었고**, 부분 설치가 성공을 보고했고, 게이트는 만들어만 놓고 **한 번도 안 썼다**.

**현재 가설**: 같은 형태가 아직 남아 있다. 확인된 것만:
- `scripts/git-hooks/pre-push`(142줄)가 **`core.hooksPath` 미설정 + 설치 코드 전역 0건**으로 존재만 하고 작동하지 않는다
- `ci.yml` ↔ `plugin-validate.yml` 의 branches lockstep 이 **게이트 없는 수동 불변식**이다 — 깨지면 `ci/**` 에 check run 이 안 생겨 ff-only 도 bypass 로 퇴화한다
- `main-entry.test.js` 의 재인라인 수렴 스캔이 `scripts/hooks/**` 만 훑어 **`bin/` 이 게이트 밖**이다

## 4. 즉시 진행할 일

### P0 — 미커밋 4파일 착지 (검증 전부 끝남)

```
 M plugins/artibot/bin/artibot-mcp.mjs                          (S-3 + O-1)
 M plugins/artibot/install.ps1                                  (S-1/S-2 주석만)
 M plugins/artibot/install.sh                                   (S-1/S-2 주석만)
?? plugins/artibot/tests/bin/artibot-mcp-stdin-utf8.test.js     (9 tests)
```
착지 절차는 §7 의 게이트 플로우 그대로. 인스톨러 2파일은 **코드 diff 0줄**(주석·빈줄 제외 `git diff -U0` 로 증명됨).

### 그 외

| # | 항목 | 근거 | 확신도 |
|:--:|---|---|---|
| 1 | **O-2 결정 필요** — `artibot-mcp.mjs#invokedFromCli` 가 M2 와 같은 심링크 false-negative. 세 안 전부 대가가 있다: ⓐ `scripts/hooks/_main-entry.js` import(`bin/`→`scripts/hooks/` 신규 의존) ⓑ 인라인 복제(M1 이 두 곳에 산 원인) ⓒ 공용 위치로 이동(리팩터 범위 큼) | 코드 확인 · **정션 재현 미실시** | 형태 실측 / 도달성 미확인 |
| 2 | **`main-entry.test.js` 수렴 스캔이 `bin/` 미포함** — O-2 본체보다 중요할 수 있다. 게이트를 만들며 못 보는 곳을 안 적은 사례 | 실측 | 실측 |
| 3 | 리뷰 잔여 **M3**(worktree `hooksPath` 신뢰경계 — 악성 PR 이 메인테이너 push 시 로컬 RCE) · **M4**(settings.json 0600→0644 강등) · **L1~L4** | `/ultrareview` 리포트 | 실측 |
| 4 | `pre-push` 배치 — `core.hooksPath` 미설정, 설치 코드 0건 | 실측 | 실측 |
| 5 | branches lockstep 게이트 신설 | 실측 | 실측 |
| 6 | `out += chunk` 패턴 **테스트 파일 전수조사** — 자식 stdout 64KB 초과 시 같은 결함. 확인된 2파일 외 미조사 | 실측(2파일만) | 부분 |
| 7 | ADR-003 LOW-2 · 학습저장소 프로젝트명 오염 · 원장 파편 11파일 | 이전 핸드오프 이월 | 커밋 작성자 측정 |

## 5. 미해결 결정/질문

### ✅ 해소 — 사이드 브랜치 게이트

**게이트가 처음으로 실제 작동했다.** 판정 기준은 `git push origin master` 에서 `Bypassed rule violations` 가 나오는지였고, **나오지 않았다.** 같은 세션 앞선 직푸시(`850b5682`)에서는 `4 of 4 required status checks are expected` 와 함께 찍혔다. 절차·전제조건은 메모리 `project-side-branch-gate-first-use-20260815` 에 있다.

### ⚠️ 미해결 — O-2 (위 §4 #1)

세 선택지 모두 트레이드오프가 있어 사용자 판단 대기 중.

### ⚠️ 미해결 — 커밋 메시지 수치 오류 (`4d8f1ad0`)

커밋 본문에 "테스트 +20건" 이라 적었으나 **실제 23건**(신규 4파일 22 + 계약테스트 순증 1). 과소집계라 위험 방향은 아니고 산술은 정합한다(`9,915 → 9,939 = +24 = 신규 23 + 기존 red 1건 전환`). 푸시 완료라 **릴리스 노트에서 교정 필요**.

## 6. 다음 세션 첫 프롬프트 후보

1. **"미커밋 4파일을 게이트 경유로 착지해줘"**
   → §4 P0. 검증이 전부 끝난 상태라 절차만 남았다. 가장 먼저 할 일.
2. **"O-2 를 어떻게 할지 정하자 — `bin/` 의존 vs 복제 vs 공용 이동"**
   → §4 #1·#2. 결정이 필요하고, 곁들여 수렴 스캔의 `bin/` 갭도 같이 닫는 게 자연스럽다.
3. **"`pre-push` 를 실제로 배치하고 branches lockstep 게이트를 만들자"**
   → §4 #4·#5. "만든 것 ≠ 작동하는 것" 이 이 리포의 반복 패턴이다.

## 7. 컨텍스트 복원 핵심 파일

- `CONTRIBUTING.md#Landing changes on master` (266-302 근처) — 게이트 플로우 + bypass 구조. **착지 절차의 진실원**
- `.github/workflows/ci.yml` · `plugin-validate.yml` 의 `push.branches` — 둘 다 `[master, main, "artibot/**", "ci/**"]`. **lockstep 이 깨지면 게이트가 무력화된다**
- `plugins/artibot/install.ps1#Request-InstallLock` / `install.sh#acquire_install_lock` — 락 패리티. 인용은 이제 심볼 기준
- `plugins/artibot/bin/artibot-mcp.mjs#fallbackStdioLoop` · `#createStdioTransport` · `#invokedFromCli`
- `plugins/artibot/scripts/hooks/_dispatcher-utils.js#readPayload` · `_main-entry.js#isMainEntry`
- `plugins/artibot/tests/dispatcher/dispatcher-payload-utf8.test.js` — NEGATIVE CONTROL 패턴의 표준
- `plugins/artibot/package.json` scripts (루트 아님)

## 8. 메타

> 생성: 2026-08-15 09:15 · `/team` 3라운드(팀원 6인) + `/ultrareview` + 게이트 착지 1회
> 커밋 2건(`850b5682` 핸드오프, `4d8f1ad0` 결함수정) · 미커밋 4파일
> 모든 수치에 측정 명령·측정자·시각 명기. 팀원 `미확인:` 은 삭제 없이 전파.

### 이번 세션의 방법론 교훈 (메모리에도 저장됨)

- **인용 오류 9건** 발생. 전부 "직접 열어보기"로만 잡혔고 **자동 게이트 검출 0건**. 스폰 프롬프트의 *"내 인용이 틀렸으면 그대로 보고하라"* 가 실제 검출 수단이었다. → `feedback-citation-relay-discipline`
- **§9 픽스처 함정이 실발동**. 198KB 픽스처가 65,536B 를 넘겼는데도 ASCII 가 오프셋을 밀어 대조군이 `fffd=0` 을 냈다. 크기 단언만 있었으면 전 파일이 공허하게 green. → `feedback-fixture-must-reach-failure-region`
- **테스트 하네스 자신이 같은 결함을 가질 수 있다.** `out += chunk` 로 자식 stdout 을 읽던 하네스가 M1 과 동일하게 손상돼, 잘못된 회귀 보고나 단언 약화로 이어질 뻔했다.
- **자기 편집이 자기 인용을 썩힌다.** 순수 주석 추가만으로 `install_hooks` 가 +13줄 밀렸다. → 주석에서는 줄번호보다 **심볼명**.

### 미확인 (삭제 없이 전파 — 다음 세션이 이 유보를 지우지 말 것)

- **인스톨러를 한 번도 실행하지 않았다.** H1 재현·락 상호배제·부분실패 종료코드는 전부 **추출 하네스**. ps1↔sh **라이브 교차 실행 미관측**
- Ctrl-C 에서 PowerShell `finally` 가 도는지 **미확인**(throw 경로만 검증)
- **win32 에서만 검증.** 심링크는 `mklink /J` 정션으로만. Linux/macOS `symlinkSync` 미확인
- `Copy-DirAtomic` 의 7개 실패 경로 중 **1개만** 실행형으로 밟음
- ps1 프로세스 종료코드는 **정적 계약으로만** 고정(최상위 `exit` 에 닿으려면 인스톨러 전체 실행 필요)
- **커버리지 게이트 85/76/85/85 미실측** — `npm run test:coverage` 이번 세션 전체에서 미실행
- `server.run()` 을 실제 `lib/mcp/server.js` 로 구동하지 않음(목 주입으로만). `t.write` 경로·백프레셔 미검증
- 제너레이터 조기 종료(`break`) 시 `decoder.end()` 미실행 경로 — 기존 flush 문과 같은 성질이라 **동작 변화는 없으나** 둘 다 미실측
- O-2 심링크 취약성은 **형태만 확인**, 정션 재현 안 함
- `inspector` 는 **테스트를 한 건도 실행하지 않았다**(23건은 정적 `it(` 카운트). RED 자기검증 4건 중 1건만 직접 재현
- `git fetch` 는 착지 직후 1회만 — 그 이후 원격 변화 미확인
