# Handoff: Artibot @ 2026-05-30

## 1. 지금 상태

| 항목 | 값 |
|------|-----|
| 브랜치 | master (origin과 동기화 — 전부 push됨) |
| HEAD | `693585b` (Artibot 브랜드 테마) |
| 최신 릴리스 | **v4.18.1** (GitHub Latest, 발행 완료) |
| 작업트리 | clean (HANDOFF/handoffs/RELEASE_NOTES_4.8_KO.md 아티팩트만 untracked) |
| 테스트 | `npm run ci` 9489+ pass (Windows 워커 crash 플래키 1건은 격리 시 통과, Linux 원격 CI 무관) |

## 2. 최근 커밋 (이 세션, 위→아래 최신순)

- `693585b` feat(theme): Artibot 브랜드 테마(Dark/Light) — /theme 피커
- `a6ae959` chore(release): **v4.18.1** — progress %·병렬 수정
- `8e45756` fix(runtime,autopilot): progress % + 병렬 팀 소환 수정 (2증상)
- `fe26ea7` feat(memory): **Dreaming** consolidation 루프 (dormant)
- `d3231cc` docs: 배선 감사 리포트 (79 confirmed gaps)
- `55013ff` feat(learning): S1 effortPolicy nightly 트레이너
- `3475136` chore(release): **v4.18.0** — effort fusion (P1/P2/P3)

## 3. 이 세션 큰 줄기 (매우 길었음)

1. **v4.18.0 릴리스**: effort 3종(P1 Score-Aware / P2 Unified Team Trigger / P3 GRPO overlay dormant) + router 분리 + README/release-check 드리프트 근본수정 + **태그 발행 메커니즘 학습**(release.yml은 `v*` 태그 트리거).
2. **배선 감사**(23유닛) → `docs/WIRING-AUDIT-2026-05-30.md` (79 gap).
3. **Dreaming 루프**: 공식 Managed-Agents dream 원칙을 Claude Code 메모리 MD에 복제. **dormant**(enabled:false). `/dreaming` 커맨드 등록됨.
4. **학습 전략 선회**: GRPO → 메모리 dreaming. 연료 없는 정책 트레이너 4개 OS 스케줄러에서 제거(grpo+session-rollup만 잔존).
5. **기능 감사(5유닛)** → 사용자 2증상 근본수정: **% 진행률 중간 멈춤**(autopilot 중간 phase 미집계 → 단조화) + **병렬 미소환**(키스톤 pluginRoot 미주입 + workflowPlan 폐기 + router shallow intent). v4.18.1로 릴리스.
6. **Artibot 브랜드 테마**: Dark/Light, `experimental.themes` 등록, marketplace mirror 복사(재시작 후 `/theme`에서 사용).

## 4. 다음 P0

> **테마2: 모델 정책 중앙 강제** — 배선 감사가 확정한 남은 진짜 갭. 정책이 agent-coordination.md + frontmatter로 흩어져 코드 enforce 없음 → Explore/워크플로우/빌트인 스폰이 정책 우회. 단일 source-of-truth + 모든 스폰 경로 참조. (이 세션에 진단·합의됐으나 미구현)

## 5. 미해결 결정/질문

- **테마 릴리스**: 693585b(테마)는 master에 있으나 v4.18.1 태그 이후 커밋 → 미릴리스. 외부 사용자가 `/theme`로 받으려면 **4.19.0 bump+태그** 필요. (이 PC는 mirror 복사로 재시작 후 사용 가능)
- **GRPO 최종 결정**: reward 캡처 훅 배선(테마1)할지 vs 완전 폐기할지. 선회 결정대로면 폐기가 일관. grpo nightly도 사실상 no-op.
- **Dreaming 활성화**: 지금 X. `/dreaming --dry-run`에서 candidates>0 나오기 시작하면(메모리 누적 후) enabled:true 검토.
- **배선 감사 백로그**: 57 unverified gap(다수 CLI-호출 false-positive 의심) + dormant(GRPO sync 캐시 미warm, cache-roi 미등록, cost-tracker 미배선). 트리아지 필요.
- **남은 캐비엇**: 기능 감사가 dormant로 분류한 GRPO routing blend(H1)/effort overlay(H2) sync 캐시가 hot path에서 warm 안 됨 — 켜도 dead. 활성화하려면 별도 수정.

## 6. 권장 첫 프롬프트

1. `/team 테마2 모델 정책 중앙 강제 구현 — agent-coordination 정책을 코드 enforce 단일 소스로, 모든 스폰 경로(Explore/워크플로우 포함) 참조`
2. `/git 4.19.0 릴리스 — 테마 + 누적 변경 bump+태그 발행 (외부 /update 가능하게)`
3. `docs/WIRING-AUDIT-2026-05-30.md 57개 unverified 갭 트리아지 — CLI-호출 false-positive 걸러 진짜 갭만`

> 다음 세션: `/resume`로 전체 복원. 메모리(MEMORY.md)에 [[project-dreaming]], [[project-learning-activation]], [[feedback-release-readme-sync]] 영속됨.
