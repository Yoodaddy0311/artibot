# Handoff: Artibot @ 2026-06-01

## 1. 지금 상태

| 항목 | 값 |
|------|-----|
| 브랜치 | master (origin 동기화, 0 ahead) |
| HEAD | `0af2591` (chore(release): v4.19.2) |
| 작업트리 | `M .artibot/SESSION-NOTES.md` + 미추적: `HANDOFF.md`·`handoffs/`·`RELEASE_NOTES_4.8_KO.md`·**`plugins/artibot/tests/hooks/swarm-sync-backend-routing.test.js`(신규, 3/4 pass)**·`.artibot/workflow-wk29hwny0-result.json`(워크플로 전체결과 백업) |
| 테스트 | 베이스라인 9585 pass (캐시값). 신규 swarm 테스트만 **3 pass / 1 fail** |
| 이번 세션 | 워크플로 `wk29hwny0`(48 에이전트·3.4M토큰·14분)로 권장프롬프트 3건 병렬 트리아지. **분석 완료, 코드 적용은 다음 세션** (사용자: /save 후 재부팅 → 1·2·3 진행) |

## 2. 최근 커밋

- `0af2591` chore(release): **v4.19.2** — Swarm 9일 sync stale 해소
- `af89006` fix(swarm): 세션 훅 git 백엔드 배선
- `149b892` test(learning): learning-diag risk 회귀 테스트
- `72bb5e2` fix(learning,swarm): risk 오탐 제거 + swarm errors 어댑터
- `2c4903e` chore(release): **v4.19.1**

## 3. 이번 세션 큰 줄기 — 워크플로 3트랙 트리아지 결과

> 전체 스펙·검증 원문: **`.artibot/workflow-wk29hwny0-result.json`** (171KB, reboot 후에도 보존). temp 원본은 휘발 가능.

### 트랙 A — CI 검증 (P0) ❌ RED 확정
- v4.19.2(`0af2591`) CI run `26687123723` = **failure**. **Node 22** 레그가 `Validate README claims (full strict gate)`에서 exit 1.
- 원인: **command count 드리프트 70 → 71**. 실제 카운트 commands=71인데 README가 70 주장.
  - `README.md` **line 51** `- **70 Slash Commands**` ❌ / **line 822** `# 70 slash commands` ❌ / line 571 `70+` ✅(통과)
- Node 24는 통과 — strict `--full` 게이트가 Node 22 레그에만 배선됨. **admin bypass가 실패를 가림** (v4.19.0/1/2 연속 bypass됨).
- **근본원인 후속**: `Sync README badges` 스텝은 배지만 동기화하고 산문 "N slash commands"는 안 건드림 → 매 릴리스 재발. 산문까지 동기화 확장 OR strict validator를 릴리스 게이트로 강제(bypass 금지).

### 트랙 B — 22 배선갭 (P1): 21건 검증 (1건 pipeline[6] structured 미반환)
**adversarial verify가 스펙 오류를 다수 적발** — 경로/라인 오류, 깨질 테스트 누락. **적용 전 스펙 교정 필수.**

- **ready-to-wire (4, low risk)**:
  - `WIRE-04` cache-roi 미들웨어 → `lib/runtime/create-artibot-agent.js` (앵커 정확. 단 middleware-config-filter.test.js·create-artibot-agent.test.js의 파이프라인 length/snapshot 단언 동시 갱신 필요)
  - `WIRE-06` smart-pipeline 조건부 미들웨어 선택 → `lib/runtime/create-artibot-agent.js`
  - `WIRE-08` autopilot cost-tracker(notePhaseCost+buildCostWarningInstruction) → `lib/autopilot/engine.js`
  - `WIRE-12` lifecycle-router(routeLifecycle/routeByContext/suggestNext) → `scripts/route-lifecycle.mjs`
- **needs-rework (1)**: `WIRE-03` subagents.js contract에 per-teammate effort/budget 노출 — **REAL gap 맞음**, 로직 정확하나 스펙이 `workflow-plan.js` 경로를 틀림(실제: `lib/cognitive/workflow-plan.js`, team return 219-225). 경로만 고치면 ready.
- **reclassify-dormant (3, 건드리지 말 것)**:
  - `WIRE-01` GRPO reward-emitter — `reward-metrics.js` lines 30-54 "DORMANT BY DESIGN" 배너. 메모리 [[project-learning-activation]] GRPO CLOSED와 일치. 재무장은 별도 maintainer 결정.
  - `WIRE-02` knowledge-transfer promote — 사실 wiring이 아니라 내부 dedup이고 `knowledge-demotion-promote.test.js` 깨뜨림. hotSwap 자체에 프로덕션 콜러 없음(상위 dormant).
  - `WIRE-19`
- 나머지 ~13건(WIRE-05,07,09~11,13~18,20~22) 판정은 결과 JSON `full[]` 배열에 있음 — 다음 세션에서 읽어 분류.

### 트랙 C — triage 문서 + swarm 테스트
- **triage 문서 결정 = move-to-tracked**: `docs/triage/teams-bucket-session-end.md`는 **애초에 존재한 적 없음**. 추적 위치는 `plugins/artibot/docs/adr/ADR-002-teams-bucket-session-end.md` (ADR 디렉터리는 .gitignore 31-32 `!...docs/adr/**`로 이미 추적됨, gitignore 수정 불필요). `/adr` 커맨드로 ADR-002 작성 권장.
- **swarm 테스트 = 미완성**: `tests/hooks/swarm-sync-backend-routing.test.js` 4케이스 중 **3 pass / 1 fail**. 실패: "non-git(http) backend STILL hits the egress gate and is blocked" (`:206`). 프로덕션 `swarm-sync.js`는 **변경 안 됨**(리팩토링 취소됨). spawnSync 서브프로세스 + `CLAUDE_PLUGIN_ROOT` fixture 방식. **1케이스 수정 후 커밋**.

## 4. 다음 P0

> **트랙 A CI 핫픽스** — `README.md` line 51·822만 `70`→`71` 수정(line 571 `70+`는 유지, 전체치환 금지) → `node plugins/artibot/scripts/ci/validate-readme-claims.js --full --verbose`로 로컬 확인 → 커밋 `fix(readme): command count 70 -> 71` → push해서 **admin bypass 없이 CI green** 확인.

## 5. 미해결 결정/질문

- **CI 산문 자동동기화**: `Sync README badges`가 산문 카운트 미동기화 → 재발 방지책(산문 동기화 확장 vs strict 게이트 강제) 결정 필요.
- **swarm 테스트 1 fail**: `:206` 케이스가 http backend에서 게이트 차단을 기대하나 실패 → fixture/expectation 둘 중 무엇이 틀렸는지 규명 후 수정.
- **ADR-002 작성 여부**: teams-bucket triage 결정을 `/adr`로 `plugins/artibot/docs/adr/ADR-002-...`에 정식 기록할지.
- **배선 스펙 신뢰도**: verify가 다수 경로/라인 오류 적발 → 각 WIRE 적용 시 스펙 맹신 말고 실제 파일 재확인 후 surgical 적용.
- **teams 버킷=0**: 여전히 platform-blocked(SessionEnd `team_config` 부재). 대기.
- **RELEASE_NOTES_4.8_KO.md**: 미추적 아티팩트 — 추적/삭제 결정.

## 6. 권장 첫 프롬프트

1. `README.md line 51·822의 "70"을 "71"로 수정(571 "70+" 유지) → plugins/artibot/scripts/ci/validate-readme-claims.js --full 로컬통과 확인 → fix(readme) 커밋·push → CI green(no bypass) 검증` — **P0, 트랙 A**
2. `swarm-sync-backend-routing.test.js :206 실패 케이스(http backend gate 차단) 진단·수정 → 4/4 green → 테스트 추적·커밋` — **트랙 C, P1 전 정리**
3. `/team .artibot/workflow-wk29hwny0-result.json 의 full[] 읽어 WIRE-04/06/08/12 배선(각 스펙 실제파일 재확인 후 surgical, 판 사이 npm test) + WIRE-03 경로교정 후 포함. WIRE-01/02/19 dormant 제외` — **트랙 B, P1**

> 다음 세션: `/resume`로 복원. 사용자 의도 = **1·2·3 순차 진행**. 전체 배선 스펙은 `.artibot/workflow-wk29hwny0-result.json`. 메모리 [[project-learning-activation]](GRPO CLOSED), [[feedback-question-recommendations]](이번 세션 ~/.claude/rules/artibot/question-recommendations.md로 always-on 고정), [[project-v4-19-release]] 영속.
