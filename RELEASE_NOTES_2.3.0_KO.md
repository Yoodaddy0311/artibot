# Artibot v2.3.0 업데이트 안내 (2026-04-08)

> 비개발자도 이해할 수 있도록 쉬운 말로 정리한 업데이트 리포트입니다.

## 한 줄 요약

**"많이 복잡해진 Artibot을 깔끔하게 정리하고, 앞으로 계속 잘 쓸 수 있도록 기초 골격을 다졌습니다."**

---

## 오늘 무엇을 했나요?

### 🎯 큰 목표

Artibot이 그동안 여러 좋은 아이디어를 계속 흡수하면서 **너무 무거워졌습니다**. 기능이 많아지는 건 좋은 일이지만, 너무 많으면 오히려 사용자가 어떤 걸 써야 할지 헷갈리고, 컴퓨터 자원도 많이 먹게 됩니다. 그래서 오늘은 **정리 작업**을 했습니다.

### 📋 진행 방식

1. **벤치마킹**: 다른 6개의 유명한 오픈소스를 분석해서 "어떻게 단순하게 만드는지" 배웠습니다.
2. **Phase 1**: 즉시 할 수 있는 작은 개선 4가지
3. **Phase 2**: 뼈대를 다시 세우는 4가지 작업 (각 작업을 4단계로 쪼개서 진행)

---

## 🎁 추가된 주요 기능 (사용자 관점)

### 1. 📏 "핑곗거리 사전" — 모든 스킬에 5줄짜리 유혹 차단 테이블 추가

**무엇인가요?**

AI 에이전트가 작업을 할 때 종종 **"이건 나중에 해도 되지 않을까?"** 하는 유혹에 빠집니다. 예를 들어:

- "테스트는 일단 코드 먼저 쓰고 나중에 추가할게요"
- "보안 검증은 외부 API만 하면 되지 않나요?"
- "이 변수는 내부에서만 쓰니까 검증 안 해도 될 것 같아요"

이런 **핑곗거리들을 미리 사전에 등록**해두고, AI가 그런 생각을 할 때마다 **"아니에요, 그 이유는 틀렸어요. 왜냐하면…"** 하고 즉시 반박할 수 있게 만들었습니다.

**효과**:
- AI가 단계를 건너뛰려 할 때 즉시 막힘
- 119개 스킬 전부에 적용 → 약 600개의 핑계+반박 쌍이 쌓임
- 실수로 중요한 단계를 놓치는 일이 줄어듦

### 2. 🗂 "툴 묶음" 시스템 — 50개 명령어를 9개 주제로 정리

**무엇인가요?**

Artibot에는 지금 50개가 넘는 슬래시 명령어(`/build`, `/test`, `/mkt`, `/seo` 등)가 있습니다. 너무 많아서 **어떤 상황에 뭘 써야 할지 헷갈립니다**.

오늘 이걸 **9개의 주제별 묶음**으로 정리했습니다:

| 묶음 | 용도 | 포함된 명령어 예시 |
|------|------|-------------------|
| 💻 **code** | 코드 작성/테스트/리뷰 | `/build`, `/test`, `/verify`, `/tdd`, `/code-review` |
| 🎨 **design** | 디자인/프레젠테이션 | `/ppt`, `/design`, `/visual-check` |
| 🚀 **devops** | 배포/Git | `/git` |
| ✍️ **content** | 콘텐츠/SEO | `/content`, `/social`, `/email`, `/seo` |
| 📢 **marketing** | 마케팅 전략 | `/mkt`, `/ad`, `/cro`, `/crm`, `/analytics` |
| 🔍 **analysis** | 분석/학습 | `/analyze`, `/explain`, `/learn`, `/repo` |
| ⚙️ **meta** | 설정/도움말 | `/sc`, `/setup`, `/permissions`, `/update` |
| 👥 **team** | 팀/협업 | `/team`, `/plan`, `/implement`, `/task` |
| 📦 **misc** | 기타 | (현재 비어있음) |

**효과**:
- 미래에 "지금은 코드 작업 모드" 같은 **상황별 필터링** 가능 (마케팅 명령어 숨김 등)
- 새 사용자가 처음 봤을 때 덜 압도됨

### 3. 🎯 "8단계 업무 흐름" — 모든 작업을 8개 단계로 매핑

**무엇인가요?**

프로그램 개발/마케팅 업무는 보통 이런 흐름으로 진행됩니다:

```
📝 Spec(요구사항) → 📐 Plan(설계) → 🔨 Build(구현)
→ ✅ Verify(검증) → 👀 Review(리뷰) → 🚀 Ship(배포)
                                     ↓
                 📢 Marketing(마케팅) + 🎨 Design(디자인)
```

**각 단계별로 "이 단계에 어울리는 전문가 에이전트"를 미리 지정**해뒀습니다:

- `Build` 단계 → `backend-developer`, `frontend-developer`, `tdd-guide` 등 8명
- `Review` 단계 → `code-reviewer`, `security-reviewer`, `spec-reviewer`, `quality-reviewer`
- `Marketing` 단계 → `marketing-strategist`, `ad-specialist`, `content-marketer` 등 6명

**효과**:
- 사용자가 "지금 내가 구현 단계인데…" 라고만 말해도 AI가 어떤 전문가를 불러야 할지 알게 됨
- 향후 "업무 흐름 추적" 기능의 토대

### 4. 📚 "규칙 사전" — 12개 도메인의 173개 규칙을 CSV로 정리

**무엇인가요?**

코딩할 때 지켜야 할 규칙들 (예: "비밀번호는 해시해서 저장하기", "SQL은 파라미터 바인딩 사용") 이 지금까지 여기저기 흩어져 있었습니다. 이걸 **엑셀 표처럼 정리**했습니다:

| 도메인 | 규칙 수 | 예시 |
|--------|--------|------|
| frontend | 15 | "img 태그에는 alt 속성 필수" |
| backend | 16 | "SQL 파라미터 바인딩 사용" |
| security | 16 | "비밀은 코드에 하드코딩 금지" |
| performance | 15 | "데이터베이스 N+1 쿼리 피하기" |
| ux | 13 | "로딩 상태 항상 표시" |
| accessibility | 14 | "키보드 탐색 가능해야 함" |
| testing | 14 | "테스트 먼저, 코드 나중" |
| devops | 14 | "시크릿은 환경변수로" |
| database | 14 | "외래키 인덱스 필수" |
| llm | 14 | "프롬프트 버전 관리" |
| typing | 13 | "any 타입 피하기" |
| patterns | 15 | "함수는 50줄 이하로" |
| **합계** | **173** | |

**효과**:
- 에이전트가 이 표를 보고 **내가 지켜야 할 규칙을 정확히 알 수 있음**
- 예를 들어 backend-developer는 `backend:rest-verbs`, `security:parameterized-sql` 같은 규칙만 골라서 참고 가능

### 5. 🔖 "에이전트 자동 등록" 시스템

**무엇인가요?**

예전에는 에이전트를 추가할 때 여러 파일을 고쳐야 했습니다. 이제는 **에이전트 파일 하나만 만들면 자동으로 등록**됩니다.

```yaml
# agents/my-new-agent.md 상단에만 써두면 끝
name: my-new-agent
capabilities: [analysis, reporting]
lifecycle: analysis
rules: [patterns:functional-programming]
```

**효과**:
- 새 에이전트 추가가 훨씬 쉬워짐
- 중복 설정 파일 제거 기반 마련

### 6. 🔧 "에이전트 자동 색인(Index)" 문서

**무엇인가요?**

`agents/INDEX.md` 파일이 자동으로 생성됩니다. 28개 에이전트 전부를 단계별로 분류한 표입니다:

```
## Build 단계 (8명)
| backend-developer | api-design, database-integration | opus |
| frontend-developer | ui-implementation, state-management | opus |
...

## Marketing 단계 (6명)
| marketing-strategist | market-analysis, gtm-planning | opus |
...
```

**효과**:
- 어떤 에이전트가 있고 무슨 역할인지 한눈에 파악 가능
- 수작업 없이 항상 최신 상태 유지

### 7. ⚡ "해시 기반 캐싱" — 세션 시작 속도 개선

**무엇인가요?**

Artibot은 세션을 시작할 때마다 119개 스킬 파일을 모두 읽습니다. 오늘은 **각 스킬에 지문(hash)을 달아놨습니다**. 세션을 두 번째 열 때, 스킬 내용이 바뀌지 않았다면 **"아, 그대로네"** 하고 다시 안 읽습니다.

**효과**:
- 세션 시작 시 불필요한 파일 읽기 감소
- 컨텍스트 토큰 절감
- 24시간마다 자동 갱신

### 8. 🛡 "훅 감사 보고서" — 현재 상태 완전 파악

**무엇인가요?**

Artibot에는 **43개의 자동화 장치(hook)**가 있습니다. 세션 시작 시, 파일 저장 시, 명령어 실행 전 등 여러 시점에 자동으로 실행됩니다. 오늘 이 43개를 **완전히 감사**했습니다:

- 어떤 훅이 어떤 시점에 실행되는지
- 각 훅이 오류 처리를 제대로 하는지
- 향후 "8개로 정리 가능한지, 아니면 꼭 따로 있어야 하는지"

**결과**: `docs/phase2/hook-audit.md`에 전체 내역 기록됨.

**효과**:
- 향후 훅 정리 작업의 안전한 기초
- "지금 뭐가 돌아가고 있는지" 투명해짐

### 9. 🔀 "훅 디스패처" — 가벼운 훅 실행 엔진 (비활성 상태)

**무엇인가요?**

43개 훅을 한꺼번에 **4개의 공식 슬롯** (`SessionStart / UserPromptSubmit / PostToolUse / PreCompact`)으로 묶을 수 있는 디스패처를 만들었습니다. 오류가 나도 다른 훅에 영향을 주지 않는 격리 구조입니다.

**중요**: 아직 **활성화되지 않았습니다**. 사용자 승인 후에 켭니다. 지금은 안전하게 옆에서 테스트 중입니다.

**효과 (활성화 시)**:
- 훅 개수 43 → 4개 슬롯으로 단순화
- 각 훅이 독립적으로 실행되어 오류 격리
- 세션 시작 속도 향상

---

## 🐛 이번에 발견하고 고친 실제 버그들

### 눈에 잘 안 띄었던 숨은 버그 7개

| # | 버그 | 영향 |
|---|------|------|
| 1 | `pattern-packager.js` 파일 끝에 **닫히지 않은 주석** (`/**`만 있고 `*/` 없음) | 테스트 파서 실패 → 74개 테스트 안 돌아감 |
| 2 | `harness-ablation.js`가 **삭제된 미들웨어를 import** 중 | 테스트 0개 실행 |
| 3 | `user-prompt-handler.js`의 정규식에 **실제 backspace 글자 (0x08)** 가 섞여있음 (타자 실수) | ESLint 에러 + 기능 버그 가능성 |
| 4 | `style-registry.test.js` mock 경로가 **한 단계 잘못 계산**됨 | 테스트 1개 실패 |
| 5 | `vitest.config.js`의 셔뱅 처리 규칙이 **`scripts/hooks/`만 커버** | `scripts/evals/`의 파일은 파싱 실패 |
| 6 | `agents/INDEX.md` 문서 파일이 **에이전트 28개를 29개로 인식하게 만듦** | 검증기 오류 |
| 7 | `harness-ablation.js` **stale test assertion** | 테스트 1개 실패 |

### Review-gate (품질 검사 훅) 자체 버그 3개

| # | 문제 | 해결 |
|---|------|------|
| 1 | 괄호 체크가 **템플릿 리터럴 `${...}` 오탐지** | Node 자체 파서(`node --check`)로 교체 |
| 2 | "테스트 없음" 오탐지 — **미러 경로(`tests/core/foo.test.js`)를 못 찾음** | 재귀 탐색 + basename 매칭 |
| 3 | `TODO/FIXME` 오탐지 — **JSDoc 주석 안의 `@example console.log(...)`까지 플래그** | 주석 영역 스킵 로직 추가 |

### Lint 경고 14건 + 경고 5건 → **0건**

프로젝트 전체 lint를 **0 errors, 0 warnings**로 만들었습니다.

---

## 📊 숫자로 보는 오늘의 성과

| 지표 | 수치 |
|------|------|
| 신규 파일 | **30+** (lib, scripts, tests, commands, docs) |
| 수정 파일 | **200+** (119 skills frontmatter + 54 commands frontmatter + 28 agents frontmatter + 기타) |
| 신규 테스트 | **144개** |
| 전체 테스트 | **5091개 → 100% 통과** |
| Lint 오류 | **0개** |
| Lint 경고 | **0개** |
| 새로 추가된 의존성 | **0개** (Node 내장만 사용) |
| 기능 삭제 | **0개** (전부 추가만) |
| 토큰 절감 설계 | 해시 캐싱으로 세션 시작 시 중복 스킬 주입 방지 |
| 벤치마킹한 오픈소스 | 6개 (rtk, obsidian-mind, ui-ux-pro-max, mcp2cli, agent-skills, hermes-agent) |

---

## 🔐 안전장치

이번 업데이트는 **"위험한 것은 아무것도 건드리지 않았습니다"**:

- ✅ `hooks.json` 파일 **한 바이트도 변경 없음** (가장 민감한 설정)
- ✅ SessionStart 훅 **정상 작동 확인** (이전에 깨졌던 이력 있는 파일)
- ✅ 모든 변경은 **추가만** — 삭제 없음
- ✅ **되돌리기 쉬움** — Feature flag로 위험한 기능은 기본 OFF
- ✅ **다 테스트 통과** — 5091개 중 0개 실패

---

## 🛣 앞으로 할 일 (사용자 승인 대기 중)

오늘 작업에는 **의도적으로 미뤄둔 3가지**가 있습니다. 위험도가 높아서 사용자가 먼저 확인한 다음에 진행해야 합니다:

| 남은 작업 | 무엇인가요? | 왜 미뤘나요? |
|----------|-------------|-------------|
| **A.4** — 라이프사이클 라우팅 활성화 | 새로운 8단계 자동 라우팅을 켜는 일 | 사용자 경험이 바뀌는 변화 — 먼저 알림 필요 |
| **C.3** — hooks.json 정리 실제 적용 | 43개 훅 → 4개 슬롯으로 마이그레이션 | 세션 시작 훅이 민감해서 단계별 확인 필요 |
| **C.4** — 사용하지 않는 훅 파일 이동 | `_deprecated/` 폴더로 이동 | C.3 완료 후에만 가능 |

**감사 보고서** (`docs/phase2/hook-audit.md`)를 먼저 읽어보시고, 각 훅에 대한 결정을 내려주시면 진행합니다.

---

## 📚 참고 — 벤치마킹한 6개 레포에서 배운 것

오늘 작업은 6개 오픈소스를 분석한 결과를 직접 적용했습니다:

| 레포 | 배운 것 | 적용한 곳 |
|------|--------|----------|
| **agent-skills** (Addy Osmani) | "핑곗거리 테이블" + 20개 스킬의 단순함 | 119 skills Rationalizations |
| **hermes-agent** (Nous Research) | 자체 등록 레지스트리 + 툴셋 조합 | toolsets.json, agent-registry.js |
| **mcp2cli** | 해시 기반 신선도 캐싱 | source_hash + skill-hash-cache.js |
| **obsidian-mind** | 4-hook 미들웨어 모델 | hook-dispatcher.js (비활성) |
| **ui-ux-pro-max** | CSV-as-Config + 심볼릭 링크 전파 | rules/csv/ 12개 도메인 |
| **rtk** | 레지스트리 기반 라우팅 + 얇은 훅 | lifecycle-router.js |

---

## 🎉 결론

Artibot이 오늘부터 **훨씬 정돈된 상태**가 되었습니다. 사용자가 당장 체감하는 변화는 크지 않을 수 있지만, **앞으로 개발이 훨씬 빨라지고, 새 기능 추가가 쉬워지며, 버그가 덜 생깁니다**.

**핵심 3가지:**

1. **"많이 늘어난 것을 정리만 하고, 기능은 전혀 안 줄였습니다"**
2. **"앞으로 계속 단순하게 유지할 수 있는 기초를 다졌습니다"**
3. **"위험한 부분은 미뤄두고, 안전한 것부터 했습니다"**

다음 단계는 사용자 결정에 달렸습니다. 언제든 편하게 요청해주세요.

---

_생성 시각: 2026-04-08_
_Artibot 버전: 2.3.0_
_총 작업 시간: 약 6시간_
_작업 방식: Claude Code 멀티 에이전트 병렬 실행_
