# Personal local config (auto-excluded — do not commit)
CLAUDE.local.md

# Build artifacts
_reports/
_benchmarks/
coverage/

# Runtime session data (top-level only — do NOT match lib/runtime/ or its subdirs)
/runtime/

# Pre-dispatcher hooks.json backup (single-use rollback artifact)
hooks/hooks.json.before-dispatcher

# Autopilot runtime boot artifact (regenerated by the autopilot runtime — never commit)
.ap-boot.mjs
