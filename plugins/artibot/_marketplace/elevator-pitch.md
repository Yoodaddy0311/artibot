# Artibot — Elevator Pitch

## 100-word value proposition (listing description)

Artibot turns Claude Code into an autonomous agent OS. It ships 28 specialist
agents, 100 skills, and 56 commands wired into an 11-stage cognitive runtime
that automatically decomposes work, dispatches parallel teams, and
cross-verifies results — no slash commands required. Lifelong learning
(GRPO + drift detection) keeps the system improving across sessions, while a
strict local-only data policy keeps every byte inside your machine. Built-in
observability (token usage, multi-session dashboard, opt-in OTEL) gives you
production-grade visibility from day one. 5,000+ tests. MIT. Zero runtime
dependencies. Ready for serious work.

> Word count: 99

## One-liner (≤ 80 chars)

Autonomous agent OS for Claude Code — 28 agents, parallel teams, lifelong learning.

## Tweet-length (≤ 280 chars)

Artibot v3.9 — the autonomous agent OS for Claude Code. 28 agents, 100 skills,
parallel teams, dual-process cognition, lifelong learning, opt-in OTEL,
local-only data policy, 5,000+ tests. MIT. Zero runtime deps. Production-grade.

## Hook variants (A/B test inputs)

| Variant | Headline |
|---|---|
| Builder | "Stop writing prompts. Start delegating work." |
| Reliability | "5,000+ tests. Zero runtime deps. One MIT license." |
| Privacy | "Your data never leaves your machine. Period." |
| Power user | "Parallel agent teams that cross-verify each other's work." |
| Cognitive | "System 1 for trivia. System 2 for everything that matters." |

## Differentiators (use in landing copy)

1. **Auto-team DNA** — the orchestrator delegates by default; you don't type `/team`.
2. **Data sovereignty** — no external DB, no third-party telemetry, ever.
3. **Self-improving** — GRPO + skill-evolver + drift-detector loop keeps quality climbing.
4. **Cross-tool** — AGENTS.md alias and export adapters for Cursor / Codex CLI / OpenCode.
5. **Production receipts** — 4,918 vitest tests, 90 % statement coverage, 0 lint errors.
