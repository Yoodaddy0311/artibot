# Voice Profile

Fill every field. An unfilled profile disables drift detection in `ai-slop-reviewer`.

---

**Profile**: `<author-or-brand-name>`
**Last updated**: `<YYYY-MM-DD>`
**Scope**: `<blog | column | case-study | thought-leadership | interview | all>`

---

## NNGroup 4-Dimension Voice

Score each axis from -2 to +2. Add a one-line reason so future-you can verify the placement is still accurate.

| Axis | Score | One-line reason |
|------|-------|-----------------|
| Humor (Funny -2 ↔ +2 Serious) | `<fill in -2..+2>` | `<why this placement>` |
| Formality (Formal -2 ↔ +2 Casual) | `<fill in>` | `<why>` |
| Respect (Respectful -2 ↔ +2 Irreverent) | `<fill in>` | `<why>` |
| Enthusiasm (Enthusiastic -2 ↔ +2 Matter-of-fact) | `<fill in>` | `<why>` |

**Example row** (delete when filling): `| Humor | +1 | Leans serious; occasional dry aside but no wordplay |`

---

## Brand Voice Additional Axes

| Axis | Placement | One-line reason |
|------|-----------|-----------------|
| Authority level (cautious explainer ↔ opinionated insider) | `<fill in>` | `<why>` |
| Warmth (clinical distance ↔ first-name warmth) | `<fill in>` | `<why>` |
| Technicality (plain-language ↔ code-and-jargon) | `<fill in>` | `<why>` |
| Formality of terminology (everyday ↔ precise) | `<fill in>` | `<why>` |
| Conviction (hedged survey ↔ sharp claim) | `<fill in>` | `<why>` |
| Humor frequency (rare grace note ↔ every few paragraphs) | `<fill in>` | `<why>` |

---

## Sentence Structures I Favor

List 3-5 sentence patterns that recur in your best work. Use fragments of real examples, not descriptions.

- `<fill in — e.g., "Short declarative sentence, then a colon: the actual point.">`
- `<fill in>`
- `<fill in>`

---

## Sentence Structures I Avoid

List 3-5 structures that do not fit the voice, with a short note on why.

- `<fill in — e.g., "Stacked qualifiers ('might potentially possibly') — conviction axis sits sharp, so hedges clash">`
- `<fill in>`
- `<fill in>`

---

## Signature Vocabulary and Phrases

Words and phrases the voice uses regularly. Not catchphrases — returning tools.

- `<fill in — e.g., "the mechanism here is...">`
- `<fill in>`
- `<fill in>`
- `<fill in>`
- `<fill in>`

---

## Forbidden Vocabulary

Words that do not belong in this voice, even when technically correct.

- `<fill in — e.g., "leverage — replaced with 'use' or 'draw on'">`
- `<fill in>`
- `<fill in>`

---

## One-Line Identity Statement

A single sentence that names what this voice is. It is read first by every writing skill.

`This voice is <fill in — e.g., "a working practitioner explaining a hard-won lesson to a peer, with conviction and no filler">`
